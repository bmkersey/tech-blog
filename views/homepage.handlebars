{{#each posts as |post|}}
<a class="list-group btn list-item main-list single-post" href="/posts/{{post.id}}">
    <span class="float-left">{{post.title}}</span>
    <span class="float-right">Posted by: {{user.username}} on {{format_date created_at}}</span>
</a>
{{/each}}